# AgentReady configuration for ansible-operator-plugins
# See https://github.com/ambient-code/agentready for documentation
#
# No attributes are excluded: openapi.yaml documents the one stable JSON API
# surface (internal/ansible/apiserver) directly, see
# docs/decisions/adr-0004-openapi-not-applicable.md.
excluded_attributes: []
